type     worse_time, best_time, av_time
bubble   n**2, n**2, n**2
insert   n**2, n, n**2
select   n**2, n**2, n**2
heap     nlog(n), nlog(n), nlog(n)
merge    nlog(n), nlog(n), nlog(n)
quick    n**2, nlog(n), nlog(n)
